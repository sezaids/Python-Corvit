----------MIQ----------
# What is nested if statement
# is difference between multiline and single line string "length and type"?
# Deep copy vs Shallow copy
# Pass by value vs Pass by reference


* Arbitrary | args
** Arbitrary Keyword arguments | kwargs



----------VIP----------
# List
# Dictionary
# Tuple
# Loop
# Conditions
# * es-teric

# ls.sort(reverse=true)

----------Library----------
kivi library is used to built Android apps
tkinter library is used to build desktop apps (.exe)


Switch one language to another in 24 Hours - Book


parameter > veriable name > i.e : def sum (a,b)
arguments > veriable value that assign when func is called > i.e : sum (10,20)

formats:
    print("hello world {} : {}".format(a,b)) > use 16 bits > less optimized
    print(f"hello world {a} : {b}") > use 8 bits > more optimized

list = [1,2,3,4,5,6,7,8,9]
tuple = (1,2,3,4,5,6)

What are DocStrings
What are PEP-8?